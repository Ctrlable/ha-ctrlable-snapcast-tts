{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Ctrlable Snapcast Streamer",
        "description": "Enter the add-on URL and bearer token from the add-on's Advanced tab.",
        "data": {
          "addon_url": "Add-on URL",
          "bearer_token": "Bearer Token"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to the add-on. Check the URL and that the add-on is running.",
      "invalid_auth": "Invalid bearer token.",
      "unknown": "Unexpected error."
    },
    "abort": {
      "already_configured": "Ctrlable Snapcast TTS is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Satellite → Snapcast Mappings",
        "menu_options": {
          "add_mapping": "Add / update mapping",
          "remove_mapping": "Remove mapping",
          "done": "Save and close"
        }
      },
      "add_mapping": {
        "title": "Add / Update Mapping",
        "description": "Map a satellite + wake word to one or more Snapcast clients. Use * as wake word to match any.",
        "data": {
          "satellite_id": "Satellite ID",
          "wake_word": "Wake Word (* = any)",
          "target_snapclient_ids": "Target Snapcast Client(s)"
        }
      },
      "remove_mapping": {
        "title": "Remove Mapping",
        "data": {
          "mapping_key": "Mapping to remove"
        }
      }
    },
    "error": {
      "required": "This field is required."
    }
  }
}
